# NanoNav PoC Dependencies - Frozen versions for compatibility

# Core ML (PyTorch nightly for TRM compatibility - install with nightly index)
# Use: pip install --pre --upgrade torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu126
torch>=2.10.0
torchvision>=0.25.0
torchaudio>=2.10.0
numpy>=2.3.0

# Visualization
matplotlib==3.10.7

# Graph algorithms (for A*)
networkx==3.5

# Progress bars
tqdm==4.67.1

# Official Samsung TRM dependencies (for submodule)
adam-atan2==0.0.3
einops==0.8.1
pydantic>=2.8.0,<3.0.0
argdantic==1.3.3
pydantic-settings>=2.4.0,<3.0.0

# Testing (optional)
pytest==9.0.1

# Additional standard library dependencies (usually included but explicit for clarity)
# typing - included in Python 3.5+
# pathlib - included in Python 3.4+
# json - standard library
# sys - standard library
# argparse - standard library
# heapq - standard library
